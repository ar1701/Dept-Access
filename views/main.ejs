<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Department Access</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * {
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        .form-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(10px);
        }

        .form-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .input-group {
            position: relative;
            margin-bottom: 1.5rem;
        }

        .input-group input, .input-group select {
            border: 2px solid #e2e8f0;
            border-radius: 10px;
            padding: 15px 20px;
            transition: all 0.3s ease;
        }

        .input-group input:focus, .input-group select:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.2);
        }

        .input-icon {
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
            color: #94a3b8;
        }

        .submit-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            padding: 15px 30px;
            border-radius: 10px;
            color: white;
            font-weight: 500;
            transition: transform 0.3s ease;
        }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
        }

        .illustration-section {
            color: white;
            padding: 2rem;
        }

        @media (max-width: 768px) {
            .illustration-section {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="container-fluid min-vh-100 d-flex align-items-center">
        <div class="row w-100">
            <!-- Illustration Section -->
            <div class="col-md-6 d-flex align-items-center justify-content-center illustration-section">
                <div class="text-center">
                    <h1 class="display-4 mb-3 fw-bold">Department Access</h1>
                    <p class="lead">School of Engineering</p>
                    <img src="/images/cusat_logo.png" alt="Illustration" class="img-fluid mt-4">
                    <div class="mt-4">
                        <p class="text-light mb-2">Already have an account?</p>
                        <a href="/login" class="btn btn-outline-light px-4 py-2">
                            <i class="fas fa-sign-in-alt me-2"></i>Login
                        </a>
                    </div>
                </div>
            </div>

            <!-- Form Section -->
            <div class="col-md-6 d-flex align-items-center justify-content-center">
                <div class="form-container p-5 m-3" style="width: 100%; max-width: 500px;">
                    <h2 class="form-header text-center mb-5 fw-bold">Sign in</h2>
                    <form id="facultyForm" method="post" action="/main">
                        <!-- Name Input -->
                        <div class="input-group">
                            <input type="text" id="name" class="form-control" name="name" placeholder="Enter full name" required>
                            <i class="fas fa-user input-icon"></i>
                        </div>

                        <!-- Department Dropdown -->
                        <div class="input-group">
                            <select class="form-select" id="department" name="department" required>
                                <option value="">Select Department</option>
                                <option>CSE</option>
                                <option>IT</option>
                                <option>SFE</option>
                                <option>CE</option>
                                <option>ME</option>
                                <option>EEE</option>
                                <option>EC</option>
                            </select>
                            <i class="fas fa-building input-icon"></i>
                        </div>

                        <!-- Designation Dropdown -->
                        <div class="input-group">
                            <select class="form-select" id="designation" name="designation" required>
                                <option value="">Select Designation</option>
                                <option>Placement Officer</option>
                                <option>HOD</option>
                                <option>Department Access</option>
                                <option>Other</option>
                            </select>
                            <i class="fas fa-briefcase input-icon"></i>
                        </div>

                        <!-- Phone Input -->
                        <div class="input-group">
                            <input type="tel" id="phone" name="phone" class="form-control" placeholder="Enter phone number" required>
                            <i class="fas fa-phone input-icon"></i>
                        </div>

                        <!-- Access Code Input -->
                        <div class="input-group">
                            <input type="text" id="accessCode" class="form-control" name="accessCode" placeholder="Enter access code" required>
                            <i class="fas fa-lock input-icon"></i>
                        </div>

                        <!-- Username Input -->
                        <div class="input-group">
                            <input type="text" id="username" class="form-control" name="username" placeholder="Enter username" required>
                            <i class="fas fa-user-circle input-icon"></i>
                        </div>

                        <!-- Password Input -->
                        <div class="input-group">
                            <input type="password" id="password" class="form-control" name="password" placeholder="Enter password" required>
                            <i class="fas fa-key input-icon"></i>
                        </div>

                        <button type="submit" class="btn submit-btn w-100 mt-4">
                            Sign in <i class="fas fa-arrow-right ms-2"></i>
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        // Check for error message and display it using SweetAlert
        const errorMessage = "<%= error || '' %>"; // Default to empty string if error is undefined
        if (errorMessage) {
            Swal.fire({
                icon: 'error',
                title: 'Oops...',
                text: errorMessage,
            });
        }
    </script>
</body>
</html>